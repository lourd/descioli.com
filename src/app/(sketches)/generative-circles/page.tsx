import { GenerativeCircles } from "./client"

export const metadata = {
  title: "Generative Circles",
  description: "A quick study exploring a nested circle design generatively",
}

export default function GenerativeCirclesPage() {
  return <GenerativeCircles />
}
